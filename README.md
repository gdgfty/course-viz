# CourseViz - 可视化选课助手

CourseViz 是一款现代化的可视化选课与排课工具，旨在帮助学生从复杂的课程数据中轻松筛选心仪课程、直观对比时间冲突，并制定完美的学期计划。本项目使用Byte Dance AnyGen生成

## ✨ 主要特性

- **⚡️ 极速数据导入**：支持解析标准的课程 JSON 数据文件，并内置示例数据，开箱即用。
- **🔍 多维筛选与搜索**：
  - 支持按 **开课院系 (Department)** 和 **上课时间 (Weekday)** 进行组合筛选。
  - 支持通过课程名称或代号进行实时模糊搜索。
- **📅 强大的可视化课表**：
  - **周视图 (Week View)**：宏观掌握一周的时间安排。
  - **日视图 (Day View)**：聚焦单日详情，支持 **并排显示 (Side-by-Side)** 时间冲突的课程，彻底解决重叠遮挡问题。
  - **纵轴缩放**：内置缩放滑块，自由调节时间轴高度。
- **🎯 精确选课**：
  - 选课粒度精确到 **班级 (Class Session)**，完美支持同一课程下的不同班次选择。
  - **合并相同课程**模式：一键合并同名同时间的课程块，悬停即可查看并选择特定班级。
- **📱 现代化界面**：
  - 响应式布局，侧边栏与课表面板均支持折叠，最大化浏览空间。
  - 基于 Tailwind CSS v4 和 shadcn/ui 构建，提供流畅的交互体验。

## 🛠️ 技术栈

本项目基于现代前端技术栈构建，注重性能与开发体验：

- **框架**: [React 19](https://react.dev/)
- **语言**: [TypeScript](https://www.typescriptlang.org/)
- **构建工具**: [Vite](https://vitejs.dev/)
- **样式**: [Tailwind CSS v4](https://tailwindcss.com/)
- **UI 组件库**: [shadcn/ui](https://ui.shadcn.com/)
- **图标**: [Lucide React](https://lucide.dev/)
- **路由**: [wouter](https://github.com/molefrog/wouter) (Hash-based routing for easy static deployment)

## 🚀 快速开始

### 前置要求

- [Node.js](https://nodejs.org/) (Version 18+)
- [pnpm](https://pnpm.io/) (推荐) 或 npm/yarn

### 本地开发

1. **克隆仓库**

   ```bash
   git clone https://github.com/your-username/course-visualizer.git
   cd course-visualizer
   ```

2. **安装依赖**

   ```bash
   pnpm install
   ```

3. **启动开发服务器**

   ```bash
   pnpm dev
   ```

   访问 `http://localhost:5173` 即可预览。

### 构建与部署

1. **构建生产版本**

   ```bash
   pnpm build
   ```

   构建产物将输出到 `dist` 目录。

2. **本地预览构建结果**

   ```bash
   pnpm preview
   ```

3. **部署**\
   本项目是纯静态网站，可以部署到任何静态托管服务：

   - **GitHub Pages**: 将 `dist` 目录内容推送到仓库并在 Settings 中开启 Pages。
   - **Vercel / Netlify**: 直接导入 Git 仓库即可自动部署。
   - **本地运行**: 使用 `python -m http.server` 在 `dist` 目录下启动。

## 📂 数据格式说明

CourseViz 接受符合以下结构的 JSON 数据：

```json
{
  "courses": [
    {
      "course_code": "CS 101",
      "course_name": "计算机导论",
      "course_department": "CS",
      "classes": [
        {
          "class_id": "1001",
          "teachers": ["张三"],
          "classroom": "E10-201",
          "time_periods": [
            {
              "start_date": "2026/03/02",
              "end_date": "2026/06/15",
              "weekday": "星期一",
              "start_time": "08:00",
              "end_time": "09:35"
            }
          ]
        }
      ]
    }
  ]
}
```

## 🤝 贡献

欢迎提交 Issue 或 Pull Request 来改进 CourseViz！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 提交 Pull Request

## 📄 许可证

[MIT](LICENSE) © 2025 Course Visualizer
